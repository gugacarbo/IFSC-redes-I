{
  "name": "@udp-iot/matriz-java",
  "version": "1.0.0",
  "private": true,
  "description": "UDP-IoT Matriz - Java implementation",
  "scripts": {
    "build": "javac -d dist -cp src src/matriz/*.java",
    "dev": "java -cp dist matriz.MatrizMain",
    "typecheck": "javac -Xlint -d dist -cp src src/matriz/*.java",
    "lint": "javac -Xlint -d dist -cp src src/matriz/*.java",
    "clean": "rm -rf dist"
  }
}
