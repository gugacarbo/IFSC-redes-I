{
  "name": "@udp-iot/filial-java",
  "version": "1.0.0",
  "private": true,
  "description": "UDP-IoT Filial - Java implementation",
  "scripts": {
    "build": "javac -d dist -cp src src/filial/*.java",
    "dev": "java -cp dist filial.FilialMain",
    "typecheck": "javac -Xlint -d dist -cp src src/filial/*.java",
    "lint": "javac -Xlint -d dist -cp src src/filial/*.java",
    "clean": "rm -rf dist"
  }
}
